#include "main.h"
#include <string.h>

/**
 * _strlen - return the length the length of a string
 * @s: string to be calculated
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
